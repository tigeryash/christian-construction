import MenuButton from "./menu-button";
import NavList from "./nav-list";
import NavMenu from "./nav-menu";

const Nav = () => {
  return (
    <nav>
      <NavList />

      <MenuButton />

      <NavMenu />
    </nav>
  );
};

export default Nav;
